# Core dependencies used by the training, evaluation, and inference scripts.
numpy
opencv-python
PyYAML
torch
tqdm
ultralytics

# Note:
# For GPU usage, install the PyTorch build that matches your local CUDA setup.
